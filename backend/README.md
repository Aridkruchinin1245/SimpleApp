1. Install python
2. pip install -r requirements.txt
3. python -m uvicorn main:app --host 0.0.0.0 --reload
4. check the link in terminal and insert in into frontend/src/services/api.js BaseUrl
5. cd frontend
6. npm install
7. npm run serve
8. use link in terminal
